<?xml version='1.0' encoding='UTF-8'?>
<!--
  Deliberately empty. Every entry in here was inherited from the template and
  has since been either deleted with the auth/server strip or migrated to
  :libraries:resources, so VerifyStrings is now a hard gate with nothing
  suppressed. Keep it that way: fix the finding or, if the surface genuinely is
  developer-facing, exclude it in detekt.yml with a reason. Do not re-baseline.
-->
<SmellBaseline>
  <ManuallySuppressedIssues/>
  <CurrentIssues/>
</SmellBaseline>
