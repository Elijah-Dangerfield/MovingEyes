applicationId=com.dangerfield.movingeyes
# The version below is rewritten by release-please when a release PR merges.
# Listing this file under extra-files is not enough on its own: without an
# annotation release-please updates nothing and reports nothing, which is how
# the tag and the shipped versionName came to disagree.
#
# It has to be the start/end block rather than a trailing marker. release-please
# only recognises an annotation inline on the same line or as a block, and the
# inline form is unusable here because Properties.load reads a trailing "# ..."
# as part of the value.
# x-release-please-start-version
versionName=1.0.0
# x-release-please-end
versionCode=1
releaseChannel=dev
buildNumber=1
